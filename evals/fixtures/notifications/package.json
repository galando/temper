{
  "name": "notifications-fixture",
  "private": true,
  "version": "1.0.0",
  "description": "Temper eval fixture — seeded defect: component built but never mounted",
  "dependencies": {
    "react": "^18.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/react": "^18.0.0"
  },
  "scripts": {
    "typecheck": "tsc --jsx react-jsx --noEmit --skipLibCheck --strict --target ES2020 --moduleResolution bundler --module ESNext src/App.tsx src/components/NotificationBell.tsx"
  }
}
