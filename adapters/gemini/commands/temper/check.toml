# AUTO-GENERATED — do not hand-edit. Regenerate via scripts/generate-gemini.sh.
# Source: commands/check.md
# Plugin version: 7.0.1
# Tier: Tier 2 — Gemini CLI extension (single-context, CLI-gated; no design/pack/eval)
# Upstream schema verified: 2026-07

description = "Run stack-aware validation pipeline"
prompt = """
# Check: Validation Pipeline

**Goal:** Auto-detect stack and run validation levels in order.

## Execution

> **Full methodology:** Read `reference/check.md`

### Quick Reference

Levels (stop on failure):
0. Environment — verify not production

1. Compile/Build
2. Unit Tests
3. Integration Tests (if configured)
4. Coverage (threshold from config)
5. Lint/Format
6. Type Check
7. Security (dependency scan)

### Active Skills

- **Context Engineering** — load hierarchical context at stage start (rules → arch → source → errors, under 2K lines/task)
- **Temper Core** — stack detection, pack resolution, quality gates

### Deterministic Gate

This is the same gate the unified `the Temper unified pipeline` command's Check stage runs — running this
command standalone must not skip it, or `temper gate commit` sees no check evidence and
wrongly blocks (or wrongly passes) a later commit. Follow
`agents/check.md` steps 2-3 (record test/coverage evidence, and
trace every `intent.md` scenario to a test via `temper evidence add --scenario`) as you
validate, then run `scripts/temper gate check` and show its
PASS/FAIL to the user via `ask the user directly and wait for their answer` (this command is not a subprocess — you own
the gate here, unlike `agents/check.md`'s \"never show a gate\" rule).

**Pass `--spec-path` explicitly** — a standalone command hasn't necessarily run `temper
state init`, so `temper state get spec_path` may be empty, which silently skips the
scenario-tracing requirement (it can't find `intent.md`) instead of failing loudly.
Always call `temper gate check --spec-path .temper/specs/{feature-slug}`.

## Gate Protocol (do not skip)

Record evidence for every claim as you work, then compute — never self-assert — the
verdict:

```
scripts/temper evidence add --stage check --claim \"<what you're proving>\" \\
  --cmd \"<the exact command you ran>\" --exit <code> --label PROVEN
scripts/temper gate check
```

Paths above are relative to this plugin's own installed root (wherever the
marketplace source resolved this repo on disk), not necessarily your project's working
directory — resolve `scripts/temper` to that location if your shell's cwd differs.
"""
