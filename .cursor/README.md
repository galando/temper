# Temper — Cursor IDE export (archived)

**Archived as of v7.0.0.** Frozen at the v5.1 feature set (per CHANGELOG v5.2.1) and
last synced at plugin v6.0.1 — this snapshot is no longer regenerated on release.
`scripts/generate-cursor.sh` still works if you want to hand-run it, but it is out of
the release pipeline; Temper v7+ (the CLI-backed gates, agents/, the collapsed config)
ships Claude Code-only. See the README's Installation section.

Version: 6.0.1 (last synced — will not track plugin.json going forward)
Do not hand-edit files in this directory — they are derived, and stale by design now.
